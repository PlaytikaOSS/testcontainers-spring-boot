spring.cassandra.contact-points=${embedded.cassandra.host}:${embedded.cassandra.port}
spring.cassandra.keyspace-name=${embedded.cassandra.keyspace-name}
spring.cassandra.local-datacenter=${embedded.cassandra.datacenter}
spring.cassandra.schema-action=CREATE_IF_NOT_EXISTS
spring.cassandra.request.timeout=15s
