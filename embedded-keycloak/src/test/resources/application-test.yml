spring:
  output:
    ansi:
      enabled: always

logging:
  level:
    com.playtika.testcontainer: info
    org.springframework.boot: info
    org.springframework.security: info
    org.keycloak: info
    root: error
